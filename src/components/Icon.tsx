const Icon = () => {
  return (
    <img
      src={'/logo192.png'}
      alt="react-logo"
      style={{
        position: 'fixed',
        bottom: 20,
        right: 20,
        width: 130,
      }}
    />
  );
};

export default Icon;
